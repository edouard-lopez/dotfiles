#!/usr/bin/env bash
export MY_SERVER="srv-sys"
export BROWSER="firefox"  # Default web browser
export CDPATH="$CDPATH:$HOME/projects"
GPG_TTY="$(tty)"; export GPG_TTY
JAVA_HOME="/usr/lib/jvm/java-8-openjdk-amd64/"; export JAVA_HOME
export PATH="$HOME/apps/:/usr/local/bin/:./node_modules/.bin/:$PATH"

function include_shell_specific() {
    if [[ $BASH = *bash ]]; then
        source includes/config.bash
    elif [[ $ZSH_NAME = zsh ]]; then
        source includes/config.zsh
    else
        echo -n "" # shell
    fi
}

include_shell_specific
source includes/backup.bash
source includes/date.bash
source includes/docker.bash
source includes/git.bash
source includes/input.bash
source includes/monitoring.bash
source includes/nodejs.bash
source includes/network.bash
source includes/packages-manager.bash
source includes/pager.bash
source includes/python.bash
source includes/terminal.bash
source includes/string.bash
source includes/utilities.bash
source includes/additional.bash